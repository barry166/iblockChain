import BlockChain from "./models/BlockChain.js";
import { keys } from "./models/rsa.js";

const chain = new BlockChain();

// chain.mine();
// chain.mine();
// console.log("余额", chain.balance());
// // 转账100
// console.log("转出200");
// chain.transfer(keys.publicKey, "aaa", 200);
// chain.mine();
// console.log("余额", chain.balance());
